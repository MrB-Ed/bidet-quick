# Bidet Quick — local Whisper voice typing for Windows
# Pinned to versions verified working on Python 3.11 + Windows 10/11 + CUDA 12 (RTX 4070)
# as of 2026-05-22. CPU-only users: same pins work, the script auto-falls-back.

# Whisper inference backend
faster-whisper==1.0.3
ctranslate2==4.4.0

# CUDA runtime DLLs (these install on every platform but only matter on Windows + NVIDIA)
# Comment these out if you're on CPU-only and want a smaller install.
nvidia-cublas-cu12==12.4.5.8
nvidia-cudnn-cu12==9.1.0.70

# Audio capture
sounddevice==0.4.7
numpy>=1.26,<2.2

# Global hotkeys + clipboard paste
keyboard==0.13.5
pyperclip==1.9.0

# Tray icon
pystray==0.19.5
pillow>=10.0,<12.0

# Native Windows toasts (winotify preferred; win10toast is the fallback)
winotify==1.1.0
win10toast==0.9
